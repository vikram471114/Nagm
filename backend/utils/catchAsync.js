/**
 * هذا الملف المساعد يقوم بـ "تغليف" دوال الكنترولر.
 * فائدته هي التقاط أي خطأ يحدث داخل دالة "async" وإرساله إلى معالج الأخطاء
 * بدلاً من التسبب في توقف الخادم بالكامل.
 */
module.exports = fn => {
    return (req, res, next) => {
        fn(req, res, next).catch(next);
    };
};